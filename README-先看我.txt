这版是在你上传确认的 formal-admin-keep-ui-watermark-split-demo-fixed-inputs(1).html 基础上，把真实后台功能接回去的修复包。

恢复/保留：
1. 图集列表可拖动排序。
2. 详情图可拖动调换顺序。
3. 详情图可任选一张作为封面图。
4. 后台自检保留。
5. 网站设置保留。
6. 水印上传页面 UI 改回你确认过的版本。
7. 水印页只上传详情图，不单独上传封面图。
8. 前端保留搜索按钮，明暗按钮为图标，关于为独立页面。
9. 不包含 data/galleries.json，不会用空数据覆盖你 R2 里的旧图集数据。

覆盖 GitHub 后，如果 R2 里的 data/galleries.json 还在，旧图集会继续读取出来。
