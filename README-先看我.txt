这是安全修复包，不包含 data/galleries.json，不会用空数据覆盖你 R2 里的旧图集数据。

重点：
1. 前端恢复读取 ./api/galleries 和 ./api/site，所以你之前存在 R2 data/galleries.json 里的图集会重新显示。
2. 保留搜索按钮。
3. 明暗主题按钮改成图标。
4. 关于页面改为独立页面，不在首页下方显示。
5. 后台保留自检、网站设置、图集列表、编辑图集。
6. 水印上传在后台的“进入详细水印上传”里。
7. 水印页只上传详情图，封面从详情图里点“封面”选择。

上传方式：
把这个包里的文件覆盖到 GitHub 仓库。
注意：不要另外上传空的 data/galleries.json 覆盖 R2 数据。
